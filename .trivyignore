# Ignore all linux-libc-dev CVEs
# This is a build-time kernel header package, not a runtime component
# All CVEs require kernel-level access and do not affect the Django app

## pkg:deb/debian/linux-libc-dev
```

---

## Or Even Better — Use `--ignore-unfixed` Flag

Looking at your scan output, notice every single entry says:
```
Status:        affected
Fixed Version: (empty)